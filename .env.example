# Local Swarm defaults
REGISTRY=localhost
OLLAMA_MODEL=tinyllama
WORKER_REPLICAS=4

# Ollama concurrency. Keep low for 4GB GPUs, then raise per load test.
OLLAMA_NUM_PARALLEL=2
OLLAMA_MAX_LOADED_MODELS=1

# Service tuning
CACHE_ENABLED=false
CACHE_SIMILARITY_THRESHOLD=0.88
CACHE_TTL_SECONDS=3600
